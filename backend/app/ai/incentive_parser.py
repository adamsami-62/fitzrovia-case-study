"""AI-powered incentive parser."""
from __future__ import annotations


def parse_incentive(raw: str) -> dict | None:
    """Stub: returns a placeholder until we wire up Claude in Step 14."""
    if not raw:
        return None
    return {"_stub": True, "raw_length": len(raw)}
